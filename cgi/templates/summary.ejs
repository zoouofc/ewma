<% if (request) { %>
    <!--
        ENGG Week Movie Archive
        Copyright (c) 2016 - <%= (new Date()).getFullYear() %> Mitchell Sawatzky, All Rights Reserved

        Request length: <%= Date.now() - request.startTime %>ms
        Request s/i: <%= `${request.cookie.session || ''}/${request.id}` %>
        Username: <%= request.user.attributes.username %>
        <% if (fullSummary) { %>
        Permissions:
            <% for (let perm of [...request.user.permissions].sort() || []) { %>
                <%= perm %><% } %>
        <% } %>
        <%= request.db.summary(request, fullSummary) %>
    -->
<% } %>
