<meta charset="utf-8" />
<meta http-equiv="content-type" content="text/html;charset=utf-8" />
<meta name="description"
    content="The ENGG Week Movie Archive is an archive of the known submissions to the annual film competition during the Schulich School of Engineering's ENGG Week at the University of Calgary." />
<meta name="keywords" content="ewma, zoo, engg, week, uofc, schulich, engineering, week, movie, archive" />
<meta name="copyright" content="2017 Mitchell Sawatzky" />
<meta name="language" content="EN" />
<meta name="robots" content="noindex" />
<meta property="og:site_name" content="https://ewma.zooengg.ca" />

<link rel="canonical" href="https://ewma.zooengg.ca" />
<link rel="icon" type="image/png" href="/static/images/favicon.png" />

<% if (request && request.stylesheets) { %>
    <% for (let sheet of request.stylesheets) { %>
        <link rel="stylesheet" type="text/css" href="<%= cacheBuster(`css/${sheet}.css`); %>" />
    <% } %>
<% } %>

<% if (request && request.scripts) { %>
    <% for (let script of request.scripts) { %>
       <script type="text/javascript" src="<%= cacheBuster(`js/${script}.js`); %>"></script>
    <% } %>
<% } %>

<% if (title) { %>
    <title><%= title %></title>
<% } %>
